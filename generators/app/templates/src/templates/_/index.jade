header#header

section(class='content')

		header
			link(rel="import" href="../components/logos-element.html")
			logos-element(frameworks='backbone,reactjs,polymer')

		.buttons.row
				<% _.forEach(libs, function(lib) { %>
				<%=  '<a href="' + lib.url + '" target="_black" data-bypass="data-bypass" class="btn btn-default btn-sm">' + lib.name + '</a>'  %>
				<% }); %>

footer#footer